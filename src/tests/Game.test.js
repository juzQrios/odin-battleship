// import Game from '../modules/Game';
// import Player from '../modules/Player';

// jest.mock('Player', () => jest.fn());

// test('game initalizes the boards and the players', () => {
//   Game.initGame();

//   expect(Player).toHaveBeenCalled();
// });
